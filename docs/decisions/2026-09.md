# Decisions and incidents — September 2026

New incidents go here. `CLAUDE.md` gets at most ONE line each, and only if it is a rule that must
hold on every turn. See the top of CLAUDE.md for why.

---

## 2026-09-10 — the Fleet page 500'd, twice, on an OPTIONAL lookup

**Operator, verbatim:** *"Why there are zeros in those 2 projects? didn't we intorduced loki also
there? if not then what is the value? we need to have full observability and full security like in
cybergod.ai for all our projects otherwise we will end up like with the jobhuntwow cyber security
issue"* — and then, after the first fix: *"Your last version destroyed the fleet status
comletely."*

### What he was looking at

klimaanlage-preise.de and s4biz.io rendered `0 requests · 0 attack-shaped · 0 visitors · 0 alerts`
on Admin -> Fleet while both were plainly serving traffic. Every count on that page came from a
FILE — the shared events log that colt-web mounts. Those two projects write to `polara_events` and
`s4biz_events`, volumes this container does not mount and must not: an `external:` volume reference
is what made colt-web undeployable on staging, and a status page is never worth coupling a deploy.

The cosmetics were the smaller half. `watch()` — the loop that applies cybergod's OWN detector and
its OWN Telegram credentials to every other project — reads through the SAME function. So klima and
s4biz could not page the operator about a scanner at all. That is not a smaller version of
cybergod's security. It is none of it, and it is the jobhuntwow shape exactly.

### The fix, and the substrate it rests on

Both projects' promtails already push to `videodead-loki-1`, the same Loki as everything else on
the box. `perseus_client` prints its telemetry to stdout, so those `evt=http` lines have been in
Loki the whole time. Reading them needs no volume mount and therefore carries none of the deploy
coupling.

`_loki_fetch(line_filter, start_ts, limit) -> (rows, answered)` became the ONE query
implementation, used by the heartbeat lookup and the traffic lookup, so the badge and the numbers
beside it cannot disagree about which Loki they asked. **`answered` is not `len(rows)`** — "Loki
says this project sent nothing" and "Loki never answered" are different facts and the second must
never render as a zero.

Two things stated rather than discovered later:

- **DEDUPE.** One request produces TWO stdout lines: the project's own telemetry and the sidecar's
  `observe()`. The key is `(evt, ts, ip, method, path)`. `ts` is whole seconds, so two identical
  requests from one address inside one second collapse into one — a bounded UNDERCOUNT, in the
  noisiest direction only.
- **PARTIAL.** The fetch is capped. A project that really exceeds the cap is reported as a FLOOR
  ("at least N"), never as a confident, wrong, too-small total.

### The outage: `Could not read the fleet status.`

Two defects, both mine, both violations of rules already written in the file above the code.

1. **Parsing sat OUTSIDE the `try`.** An unexpected envelope (`doc` not a dict) raised
   `AttributeError` straight out of `_loki_fetch`, past every caller, into a 500. An OPTIONAL
   lookup was able to take the whole page down. The parse now lives inside the `try`, and `rows` is
   bound before it, because a name whose binding depends on which branch of a `try` ran is one edit
   away from a `NameError` — which here is another 500.

2. **The request path BLOCKED on Loki.** Two own-log projects x three candidate selectors x a 3s
   timeout, on 24-hour / thousand-line queries instead of the heartbeat's 15-minute / 200-line one:
   up to eighteen seconds on a cold cache. The comment on the beat cache four functions up had
   already written the rule — *a status page that hangs is its own outage* — and the change ignored
   it. That is defect class 33 against my own file, in the same file.

   `status()` now only ever READS a cache. A daemon thread fills it (`_loki_events_async`, one
   refresh in flight, guarded by a `busy` flag); the background alerting loop passes `block=True`,
   because it runs every 300s in an executor and has the time the page does not — and an alert
   computed from an empty cache is not a quiet project, it is a scanner nobody was told about.
   A cold cache renders `elsewhere` for one page load and the numbers appear on the next.

`PERSEUS_LOKI_EVENTS=0` is a kill switch, added because the only way back from the first version
was a redeploy.

### The third defect, found while fixing the second

`status()` read the cache TWICE — once through `_tail_events` for the counts, once directly to
decide which projects Loki had answered for. A background refresh landing between the two reads
would badge a project `live` while its numbers came from the older, emptier snapshot: `live` beside
0 requests, a confident wrong number, worse than the honest `elsewhere` it replaced. One read is
taken first and handed down (`_tail_events(loki_rows=...)`), and the test asserts the READ COUNT,
which is deterministic, instead of racing the thread.

### Tests

`test_the_page_never_waits_on_loki` fails if any traffic query is issued on the request thread; the
heartbeat query is exempt BY URL rather than by silence, because it is the small bounded one that
was always inline and never hung. `_rows_after_refresh()` primes the cache synchronously, so no
count assertion depends on a daemon thread finishing — a test that does is a coin flip that teaches
the operator to re-run a suite rather than read it.

**Rules earned:** an optional lookup may make a page more accurate, never less, and never slower
than the page's own budget; and `answered` is not `len(rows)`.

### The third ship, and the real defect underneath all of them

**Operator:** *"Please fix your shit the last two versions completely destroyed the page."*

The page was still dead after a deploy in which **663 tests passed, 42 staging checks passed, and
four models voted GO**. That is the finding worth keeping. Everything above was a symptom.

Three things were wrong with how this was being verified, and all three are already rules in
CLAUDE.md that I broke in this file:

1. **The whole suite tested a fixture, not the subject** (class 12). Every test in `test_fleet.py`
   calls `_setup()`, which repoints `EVENTS`, `BEAT_DIR` and `BLOCKLIST` at a tmp directory and
   DELETES `LOKI_URL`. Not one test ever called `status()` with the environment the container has.
   A suite shaped like that cannot go red no matter what the real path does.
   `test_status_answers_with_the_MODULE_DEFAULTS_not_only_a_fixture` now calls it with the module's
   own defaults.

2. **The deploy verified liveness, not the feature** (class 34). `/api/me -> 401` proves the
   process is up and the gate is on. It proves nothing about a route that COMPUTES something, and
   the admin pages were never touched by any verifier. The deploy now runs
   `docker exec colt-web python3 -c "... fleet.status() ..."` with the real `.env` and the real
   mounts, and `set -e` makes a failure fail the ship.

3. **The failure could not describe itself** (class 26). Any exception became a 500 with an empty
   body, and the page rendered the same eight words every time. The handler now catches, prints the
   traceback to stdout, and returns a rendered page carrying the exception type, message and last
   frames — with `projects` EMPTY, because a fleet page that invents rows is the defect this module
   exists to prevent.

Also: `int(os.environ.get(...))` at module level was a latent version of the same trap. The route
does `from . import fleet` inside the handler, so one non-numeric env var is a 500 on every request
with no clue as to why. `_num()` falls back and prints `fleet_config_bad` naming the variable.

### The retreat, and the observer that was never observed

**Operator:** *"Look at the last two versions all the changes that you did... remove or fix the
issue related to this bug. Also we need to have some logging and monitoring of the page itself for
the fleet part... we don't have visibility that that is what causing the issue now."*

Both halves are right, and the second explains the first.

**THE RETREAT.** The Loki traffic lookup is the ONLY thing the last two ships put on the request
path, and the Admin page was dead for both. It has never once been observed working. So
`LOKI_EVENTS_ON` now defaults to **off**, and `status()` takes exactly the path the operator last
saw working: klima and s4biz render `elsewhere` with `active` sidecars proven by their stdout
heartbeat, which was already working in production. The counts go back to being honestly
unavailable rather than dishonestly absent. It is re-enabled per box with
`python set_secret.py PERSEUS_LOKI_EVENTS` (value `1`) once the telemetry shows what the endpoint
actually does — no redeploy. Two tests hold the line: one asserts the default is off and that no
traffic query is issued while it is, the other asserts the retreat did **not** take the heartbeat
lookup with it, since removing that would make the page worse than the version being retreated to.

**THE OBSERVER WAS THE ONE UNOBSERVED THING.** Every request on this box is logged except the page
whose entire job is observing. That is defect class 1 aimed at itself, and it is the direct reason
three ships were spent guessing. `fleet._emit()` now writes one `evt=fleet_status` line per call
through `telemetry.emit` — the SAME writer as access logging, stdout to Loki plus the events log,
not a second implementation — carrying duration, project count, the state histogram, and whether
the Loki lookup was on, answered, partial, and how stale its cache is. Failures emit the same key
with `outcome=error` so one query shows both. It can never raise.

Two consequences that had to be handled rather than discovered:

- **The page must not count its own telemetry as its own traffic.** Those lines land in the events
  log `status()` reads, and the browser polls every 30 seconds, so colt-web would have accrued
  ~2,880 self-inflicted lines a day. `fleet_status` and `perseus_beat` are excluded from the count,
  which is the heartbeat rule one `evt` name over.
- **The watch loop now pages when the page itself breaks**, edge-triggered, with the recovery
  announced. It previously printed one line into a log nobody was reading while the operator found
  out by opening the page.

**Rule earned:** the observer must be observed, and a feature nobody has seen working is off.

## 2026-09-10 — the security brain has never run, and one line is why

**Operator:** *"I explicitly said to you many times that I need shield.py to be present in every
project and I also want it to act automatically on its own behalf with no human in the loop at all
using the 4 LLM consensus algorithm per incident/scan ... update the defences on daily and weekly
basis and act as an Automated SOC for those resources. I do not understand why those were not
incorporated."*

He is right that none of it is operating, and the cause is not an architectural disagreement.

    ExecStart=/usr/bin/docker exec colt-web python3 /opt/perseus/perseus/hub.py --cycle

`perseus.py` installs the package to `/opt/perseus` **on the host**. The unit executes it **inside
colt-web**, and `docker-compose.web.yml` mounts exactly two volumes into that container:
`colt_webdata:/data` and `colt_events:/var/log/colt`. `/opt/perseus` does not exist there. Every
nightly run since installation has died on "No such file or directory".

**THE CHAIN, ALL THE WAY TO THE CONSEQUENCE.** `hub.cycle()` does `rs["cycle"] = old + 1`, so the
first successful cycle publishes cycle **1**. All five projects report `enforcing cycle 0`.
Therefore no cycle has ever completed, therefore `publish()` has never written
`perseus_blocklist.json`, therefore `client.check()` iterates an empty pattern list and returns
allow — on cybergod, jobhuntwow, jev, klima and s4biz alike. The four-model consensus, the
promotion gate (24h in detection, 3-of-4 quorum, one hostile match, zero clean hits) and the
AbuseIPDB reporting were all written and **not one of them has executed in production**.

The only real defence in the estate is `shield.py`, which runs in colt-web only. That is why
cybergod shows 1,163 attack-shaped requests and is still standing while the other four are sensors.

**WHY NOTHING SAID SO.** Three checks that could not fail, stacked:

- the installer printed `TIMER_OK`, which reports that systemd will CALL something, never that the
  something works;
- its state line ran `ls /var/log/colt/perseus_*.json` **on the host**, which is not the volume, so
  it printed `(no state yet - first run)` on every ship for weeks. A message printed on every run
  means it has never once executed — defect class 2, in this repo's own installer;
- `publish()` was wrapped in `except: pass` and its return value discarded by the caller, so the
  one consequence the whole cycle exists to produce could fail nightly in silence.

**FIX (step one — the unblock).** `webapp/Dockerfile` now `COPY perseus /opt/perseus/perseus`, so
the artifact carries its own brain and there is no host path to drift; `perseus` added to
`deploy_web_direct.INCLUDE`, because a COPY whose source is not packed fails the build (the loud
version of the same mistake, and the fourth wiring point per defect class 35); `publish()` prints
`evt=perseus_publish` with `ok`, `cycle` and pattern count on both paths; and the installer now
asks the CONTAINER, at the path the five clients actually read, for a blocklist with `cycle >= 1`
newer than 48 hours, bootstrapping one cycle if none exists and exiting non-zero as `BRAIN_DEAD`
otherwise. Under `set -e` that fails the ship.

**THE SECOND REASON, found while fixing the first.** `collect()` shells out over ssh and runs
`docker ps` / `docker exec` to find Loki. That works from the operator's PC and cannot work where
the hub now runs: colt-web has no docker socket and no `recover.py` in its image. So `collect()`
returned nothing, `cycle()` hit its `NO EVIDENCE - nothing changed` early return — **which sits
before `publish()`** — and the blocklist was never written even on a night when the package was
present. Two independent causes, stacked, either one sufficient.

Both fixed: `_loki_direct()` queries Loki over HTTP from inside the container using the `LOKI_URL`
colt-web already holds on `videodead_appnet`, the same substrate `fleet.py` reads, with no socket
and no new credential; the ssh path stays as the fallback for `python perseus.py` from the
operator's machine. And the no-evidence path now publishes anyway: "we learned nothing new today"
is not a reason to leave five sites holding no ruleset. It cannot invent a rule, because nothing is
promoted on that path.

**DELIVERED IN THE SAME CHANGE** (three agents, in parallel, each briefed on this file's rules):
shield-grade detection moved into `perseus/client.py` so all five projects get the real detector
rather than a pattern list, with `shield.py` resolving the same table so the two cannot drift; a
weekly timer (Sunday 05:20 UTC) that re-vets every live rule against today's routes and retires
rules a month of traffic never matched; `perseus/incident.py`, a 10-minute watch that asks the four
vendors about ONE live burst and can only ever propose into DETECTION, capped at 12 incidents and
$0.30 a day, metered through `llm_meter`; and a SOC panel on the Fleet page that answers what is
actually enforced, whether the brain has published, and whether an attack here would reach anyone —
rendering unknown as a word, never as a zero.

**STILL OWED, and named rather than implied:** shield-grade detection in every project rather than
a pattern list, a weekly cycle beside the daily one, and per-incident consensus. Those are built on
top of a brain that publishes. Building them first would have added four more inert features to a
system whose enforcement path has never once been armed.

---

## 2026-09-10 — the deploy restarted the shared proxy on every ship, sometimes

Found by reading the ship log after the fleet page was already fixed, not by an alert.

    --- TIMER ---   -- first run:
    OK  live config valid · proxy running · :443 bound · stale mount repaired by restart (now cdcf8eaf3c37)
    ...
    videodead-caddy-1    Up 20 seconds

Against `Up 51 minutes` and `Up About an hour` on the two previous ships that hour. The shared
reverse proxy — the process holding :443 for cybergod, jobhuntwow, jev, klima, s4biz and godeyes —
had been restarted mid-deploy.

**CAUSE.** `deploy_web_direct.py` edited the live shared Caddyfile with `sed -i`. That is not an
in-place edit: sed writes a temp file and renames it, so the file gets a NEW INODE. And
`/etc/caddy/Caddyfile` is a single-FILE bind mount, which follows the OLD inode. The container was
then reading a file nobody could see, caddyguard's 10-minute timer noticed, and a restart is the
only way to re-resolve a single-file mount.

`deploy/caddyguard/agent.py` has said so in its own module docstring the entire time — *"`mv` onto
the target swaps the inode and the container silently keeps reading the OLD file forever"* — and
writes through `write_inplace()` for exactly this reason. The deploy was the one writer that
ignored the rule. Defect class 33, inside one repo rather than across two.

**WHY IT LOOKED FINE.** Whether a ship dropped every site for a few seconds depended on whether the
timer fell between the `sed -i` and caddyguard's own assemble. Three ships in the same hour did not
trip it; the fourth did. A coin-flip outage is worse than a reliable one, because the logs teach
you they are noise.

**FIX.** `sed` without `-i` is a filter and touches nothing; the result is collected in a variable
and written with `> "$CF"`, which truncates the EXISTING inode. Guarded by a size floor first — a
command substitution that fails returns the empty string, and blanking the config that serves six
domains is a worse failure than the one being fixed.
`test_the_deploy_never_replaces_the_shared_caddyfile_inode` asserts the write, not the comment.

---

### Resolved, and what the first telemetry line said

Operator, after the ship: *"this version returned back the fleet"*. The deploy's own in-container
check printed the measurement rather than an assurance:

    {"evt": "fleet_status", "outcome": "ok", "ms": 406, "projects": 5,
     "states": {"live": 3, "observed": 0, "silent": 0, "elsewhere": 2},
     "loki_events_on": false, "loki_ok": false, "loki_partial": false, "loki_rows": 0}

Which is exactly the known-good page: cybergod, jobhuntwow and jev LIVE; klima and s4biz ELSEWHERE
with active sidecars proven by their stdout heartbeat. 406 ms is the heartbeat query, which is
inline, bounded to 15 minutes and 200 lines, and is the one Loki call that never hung.

That first line also carried `loki_cache_age_s: 1789033753` — seconds since 1970 wearing a
cache-age label, because `now - 0.0` is not an age. It reports `None` when the cache has never been
filled. Trivial to fix and worth recording: this page refuses to print unmeasured numbers for the
projects, and its own telemetry does not get an exemption. The telemetry found a defect in the
telemetry on its first run, which is the argument for having built it.

**STILL OPEN, deliberately.** klima and s4biz show no traffic counts and cannot page the operator
about a scanner, because the lookup that would do it is off. `python fleet.py` reads their own logs
over ssh in the meantime. Turning it on is now an evidence-led step rather than a hopeful one: let
`evt=fleet_status` accumulate, then `python set_secret.py PERSEUS_LOKI_EVENTS` (value `1`) on one
box, and read `loki_ok`, `loki_rows`, `loki_partial` and `ms` from the same line to see whether it
works and what it costs — instead of finding out from a dead page.

---

## 2026-09-10 — the rollout excluded the project that renders the page

**Operator:** *"i still see 2 are missing"*, then *"there is a progress but still not complete I do
not understand what seems to be the problem with nailing those two items at once?"*

`perseus.py --rollout` deployed five projects and reported `5 deployed / 0 failed`, and klima and
s4biz still rendered `elsewhere / not installed`. Cause: cybergod itself was not in `ROLLOUT`, and
cybergod is the project that SERVES the Fleet page — so the fixed `fleet.py` was never deployed.

The comment justifying the exclusion claimed that including cybergod "would recurse". That claim
was FALSE and was disproved by grepping `ship.py`, which calls `perseus.py --clients` and
`--install-only` and never `--rollout`. It cost the operator two rounds. `cybergod.ai (the Fleet
page itself)` is now the LAST entry in `ROLLOUT`, and
`test_ship_py_never_invokes_the_rollout_which_is_the_real_recursion_guard` asserts the actual guard
instead of a comment. The test that encoded the old doctrine was rewritten with its old reasoning
kept in the docstring, per defect class 17.

The diagnosis came from the badge STRING, not from inference: `not installed` is a string the
current `fleet.py` cannot produce, so the running code was old. **Read what the page printed, not
what the deploy log claimed.**

---

## 2026-09-09 — CLAUDE.md was costing ~160,000 tokens per turn

**Operator:** *"can you please change the claude.md file like you mention in order to fix the `This
conversation is too long to continue. Start a new session, or remove some tools to free up space.`"*

CLAUDE.md had reached 646 KB / 7,950 lines / 307 sections and is re-injected on EVERY turn, so it
spent ~160k tokens before the operator typed anything. That is the error, in full.

`docs_split.py` writes the current file byte-for-byte into `docs/decisions/HISTORY-*.md`, VERIFIES
the copy ends with the original exactly, refuses to touch anything if it does not, and only then
MOVES `docs/CLAUDE.core.md` into place. A move, not a copy: two files holding the same doctrine is
the "one value, several homes" defect this repository has paid for more than any other, and the
newer home always loses.

Measured: `636.3 KB / ~162,894 tokens` -> `34.3 KB / ~8,778 tokens`; history 652,499 bytes.

The lean file was written by hand, never generated. Deciding which doctrine must hold on every turn
is a judgement, not a transform, and a script that guessed would quietly drop a rule.

`tests/test_claude_md_size.py` fails the build if it grows back. It imports the budget from
`docs_split.BUDGET` by PATH, and it also asserts `size > 5_000` — a size test that an empty or
truncated file could pass is not a check.
