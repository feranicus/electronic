# cybergod.ai — auto-TLS reverse proxy to the colt-web container. Used only when nothing
# else owns 80/443 (provision_web.py detects this and otherwise merges into the existing proxy).
cybergod.ai, www.cybergod.ai {
	encode zstd gzip
	reverse_proxy web:8000
}
